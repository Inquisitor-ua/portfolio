(function () {
    "use strict";
    document.addEventListener("DOMContentLoaded", function () {
        if (window.mountSkillsMeters) window.mountSkillsMeters();
        if (window.mountProjectsFilter) window.mountProjectsFilter();
    });
})();
